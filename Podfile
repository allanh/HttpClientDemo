# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'
platform :ios, '9.3'

target 'HttpClientDemo' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for HttpClientDemo
  pod 'Alamofire'
  pod 'AlamofireObjectMapper'

  target 'HttpClientDemoTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HttpClientDemoUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
